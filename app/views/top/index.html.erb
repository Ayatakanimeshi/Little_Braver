<!-- app/views/home/index.html.erb -->
<div class="container mx-auto text-center">
  <h1 class="text-4xl font-bold mt-10 mb-5">Little Braver</h1>

  <% unless user_signed_in? %>
    <div class="mt-8">
      <%= link_to 'ぼうけんのしょをつくる', new_user_registration_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to 'つづきから', new_user_session_path, class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded ml-4" %>
    </div>
  <% end %>
</div>
