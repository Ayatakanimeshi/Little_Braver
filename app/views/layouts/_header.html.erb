<!-- app/views/layouts/_header.html.erb -->
<nav class="bg-gray-800 w-full fixed top-0 left-0 z-10">
  <div class="container mx-auto flex justify-between items-center p-4">
    <div class="text-xl font-bold text-white flex-1 text-center">
      <%= link_to "Little Braver", root_path %>
    </div>
    <div class="flex-1"></div>
    <div class="flex-1 text-right">
      <% if user_signed_in? %>
        <span class="text-gray-300 mr-4">こんにちは、<%= current_user.name %></span>
        <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-300 hover:text-white" %>
      <% else %>
        <%= link_to "新規登録", new_user_registration_path, class: "text-gray-300 hover:text-white" %>
      <% end %>
    </div>
  </div>
</nav>
