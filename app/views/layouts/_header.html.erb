<!-- app/views/layouts/_header.html.erb -->
<nav class="bg-gray-800 w-full fixed top-0 left-0 z-10">
  <div class="container mx-auto flex justify-between items-center p-4">
    <div class="flex-1"></div>
    <div class="flex-1 text-center">
      <%= link_to "Little Braver", root_path, class: "text-xl font-bold text-white hover:text-gray-200" %>
    </div>
    <div class="flex-1 text-right">
      <% if user_signed_in? %>
        <%= link_to "酒場", bravers_path, class: "text-white hover:text-gray-200 mr-4 font-bold" %>
        <%= link_to "ギルド", new_braver_path, class: "text-white hover:text-gray-200 mr-4 font-bold" %>
        <span class="text-white mr-4"><%= current_user.name %></span>
        <%= link_to "ぼうけんをやめる", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white hover:text-gray-200 font-bold" %>
      <% else %>
        <%= link_to "ぼうけんのしょをつくる", new_user_registration_path, class: "text-white hover:text-gray-200 font-bold" %>
      <% end %>
    </div>
  </div>
</nav>
